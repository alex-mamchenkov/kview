/**
 * Shared UI tokens for resource detail drawers.
 * Use these so drawer width, spacing, and layout stay consistent.
 */

/** Content width in px for standard resource drawers (e.g. Pod, Service, Deployment). */
export const RESOURCE_DRAWER_WIDTH = 820;

/** Content width in px for narrow drawers (e.g. Helm Chart summary). */
export const RESOURCE_DRAWER_WIDTH_NARROW = 620;

/** Padding applied to the drawer content container (MUI theme spacing units). */
export const RESOURCE_DRAWER_PADDING = 2;

/** Vertical margin for the header divider (MUI theme spacing units). */
export const RESOURCE_DRAWER_HEADER_DIVIDER_MY = 1;
